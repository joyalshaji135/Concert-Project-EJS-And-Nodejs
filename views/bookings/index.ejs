<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book <%= concert.name %> | ConcertHub</title>
  <!-- CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #4361ee;
      --primary-dark: #3a0ca3;
      --secondary: #4cc9f0;
      --danger: #f72585;
      --success: #38a169;
      --warning: #f8d210;
      --dark: #0f0f1b;
      --darker: #0a0a12;
      --light: #f8f9fa;
      --lighter: #ffffff;
      --muted: #6c757d;
      --glass: rgba(255,255,255,0.05);
      --glass-border: rgba(255,255,255,0.1);
    }
    
    body {
      background: linear-gradient(135deg, var(--dark) 0%, var(--darker) 100%);
      color: var(--lighter);
      font-family: 'Montserrat', sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    .text-primary {
      color: var(--primary) !important;
    }
    
    .text-secondary {
      color: var(--secondary) !important;
    }
    
    .text-muted {
      color: var(--muted) !important;
    }
    
    .text-light {
      color: var(--light) !important;
    }
    
    .text-dark {
      color: var(--dark) !important;
    }
    
    .concert-details-card, .booking-card {
      background: rgba(30,30,46,0.8);
      backdrop-filter: blur(10px);
      border: 1px solid var(--glass-border);
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 8px 32px rgba(0,0,0,0.2);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      color: var(--light);
    }
    
    .concert-details-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 40px rgba(0,0,0,0.3);
    }
    
    .concert-main-image {
      height: 350px;
      object-fit: cover;
      width: 100%;
    }
    
    .card-header {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%) !important;
      border-bottom: none !important;
      padding: 1.5rem;
      color: var(--lighter);
    }
    
    .meta-item {
      display: flex;
      gap: 1rem;
      margin-bottom: 1.5rem;
      align-items: flex-start;
      color: var(--light);
    }
    
    .meta-item i {
      font-size: 1.25rem;
      color: var(--secondary);
      margin-top: 0.25rem;
    }
    
    .meta-item strong {
      display: block;
      font-weight: 600;
      color: var(--secondary);
    }
    
    .meta-item p {
      margin-bottom: 0;
      font-weight: 500;
      color: var(--light);
    }
    
    .form-control, .form-select {
      background: var(--glass);
      border: 1px solid var(--glass-border);
      color: var(--light);
      padding: 0.75rem 1rem;
      border-radius: 10px !important;
    }
    
    .form-control::placeholder {
      color: var(--muted);
    }
    
    .form-control:focus, .form-select:focus {
      background: var(--glass);
      color: var(--light);
      border-color: var(--secondary);
      box-shadow: 0 0 0 0.25rem rgba(76, 201, 240, 0.25);
    }
    
    .btn-primary {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
      border: none;
      padding: 0.75rem;
      font-weight: 600;
      border-radius: 10px;
      transition: all 0.3s ease;
      color: var(--lighter);
    }
    
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(67, 97, 238, 0.4);
      color: var(--lighter);
    }
    
    .badge {
      font-weight: 500;
      padding: 0.5rem 0.75rem;
      border-radius: 8px;
    }
    
    .badge.bg-warning {
      background: linear-gradient(135deg, var(--warning) 0%, #f9b208 100%) !important;
      color: var(--dark) !important;
    }
    
    .price-display {
      background: var(--glass);
      padding: 1.5rem;
      border-radius: 12px;
      margin: 1.5rem 0;
      color: var(--light);
    }
    
    .price-display p {
      font-size: 1.5rem;
      margin: 0;
      display: flex;
      justify-content: space-between;
      color: var(--light);
    }
    
    #totalPrice {
      color: var(--secondary);
      font-weight: 700;
    }
    
    .toggle-description {
      border-radius: 8px;
      font-size: 0.8rem;
      padding: 0.25rem 0.75rem;
      color: var(--secondary);
      border: 1px solid var(--secondary);
      background: transparent;
    }
    
    .additional-details .badge {
      background: var(--glass);
      border: 1px solid var(--glass-border);
      color: var(--light);
    }
    
    #ticket-error, #error-message {
      background: rgba(247, 37, 133, 0.15);
      border: 1px solid rgba(247, 37, 133, 0.3);
      border-radius: 10px;
      color: var(--light);
    }
    
    #success-message {
      background: rgba(56, 161, 105, 0.15);
      border: 1px solid rgba(56, 161, 105, 0.3);
      border-radius: 10px;
      color: var(--light);
    }
    
    .alert i {
      margin-right: 0.5rem;
    }
    
    /* Custom scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }
    
    ::-webkit-scrollbar-track {
      background: rgba(255,255,255,0.05);
    }
    
    ::-webkit-scrollbar-thumb {
      background: rgba(76,201,240,0.5);
      border-radius: 4px;
    }
    
    ::-webkit-scrollbar-thumb:hover {
      background: var(--secondary);
    }
    
    .input-group-text {
      background: var(--glass);
      border: 1px solid var(--glass-border);
      color: var(--secondary);
    }
    
    .card-title {
      color: var(--lighter);
    }
    
    .form-label {
      color: var(--light);
      font-weight: 500;
      margin-bottom: 0.5rem;
    }
  </style>
</head>
<body>
  <header class="bg-dark text-white sticky-top shadow-lg header" style="width: 100vw; margin-left: calc(-50vw + 50%); background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%); border-bottom: 1px solid rgba(255,255,255,0.1);">
    <div class="container-fluid px-0">
        <div class="px-4 py-3">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="m-0">
                    <a href="/bookings" class="text-white text-decoration-none d-flex align-items-center" style="text-shadow: 0 2px 4px rgba(0,0,0,0.3);">
                        <i class="fas fa-music me-2" style="font-size: 1.75rem; color: #4cc9f0;"></i>
                        <span class="fw-bold" style="font-family: 'Montserrat', sans-serif; letter-spacing: 1.5px; font-weight: 700 !important; font-size: 1.8rem;">ConcertHub</span>
                    </a>
                </h1>
                
                <div class="d-flex align-items-center">
                    <!-- User Navigation -->
                    <div class="navbar-nav">
                        <% if (user) { %>
                            <div class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" style="cursor: pointer; transition: all 0.3s ease;">
                                    <div class="avatar me-2" style="width: 38px; height: 38px; background: linear-gradient(135deg, #4cc9f0 0%, #4361ee 100%); border-radius: 50%; display: flex; align-items: center; justify-content: center; box-shadow: 0 2px 8px rgba(0,0,0,0.2);">
                                        <span class="text-white" style="font-weight: 600;"><%= user.username.charAt(0).toUpperCase() %></span>
                                    </div>
                                    <span class="me-1" style="font-weight: 500;">
                                        <%= user.username %>
                                        <% if (user.role === 'admin') { %>
                                            <span class="badge bg-gradient ms-1" style="background: linear-gradient(135deg, #f72585 0%, #b5179e 100%);">Admin</span>
                                        <% } %>
                                    </span>
                                    <i class="fas fa-chevron-down ms-1" style="font-size: 0.8rem; opacity: 0.7;"></i>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0" style="background: rgba(30,30,46,0.98); backdrop-filter: blur(10px); border: 1px solid rgba(255,255,255,0.1); border-radius: 12px; overflow: hidden;">
                                    <li><a class="dropdown-item text-white" href="/profile" style="padding: 0.5rem 1.5rem; transition: all 0.2s ease;"><i class="fas fa-user me-2" style="color: #4cc9f0;"></i> Profile</a></li>
                                    <li><a class="dropdown-item text-white" href="/settings" style="padding: 0.5rem 1.5rem; transition: all 0.2s ease;"><i class="fas fa-cog me-2" style="color: #4cc9f0;"></i> Settings</a></li>
                                    <% if (user.role === 'admin') { %>
                                        <li><hr class="dropdown-divider" style="border-color: rgba(255,255,255,0.1);"></li>
                                        <li><a class="dropdown-item text-white" href="/admin" style="padding: 0.5rem 1.5rem; transition: all 0.2s ease;"><i class="fas fa-lock me-2" style="color: #f72585;"></i> Admin Panel</a></li>
                                    <% } %>
                                    <li><hr class="dropdown-divider" style="border-color: rgba(255,255,255,0.1);"></li>
                                    <li><a class="dropdown-item text-white" href="/logout" style="padding: 0.5rem 1.5rem; transition: all 0.2s ease;"><i class="fas fa-sign-out-alt me-2" style="color: #f72585;"></i> Logout</a></li>
                                </ul>
                            </div>
                        <% } else { %>
                            <div class="d-flex">
                                <a href="/login" class="btn btn-outline-light me-2 hover-scale" style="border-color: rgba(255,255,255,0.2); transition: all 0.3s ease; padding: 0.5rem 1.25rem; border-radius: 8px;">
                                    <i class="fas fa-sign-in-alt me-1"></i> Login
                                </a>
                                <a href="/register" class="btn btn-success hover-scale" style="background: linear-gradient(135deg, #4cc9f0 0%, #4361ee 100%); border: none; transition: all 0.3s ease; padding: 0.5rem 1.25rem; border-radius: 8px; box-shadow: 0 4px 12px rgba(76,201,240,0.3);">
                                    <i class="fas fa-user-plus me-1"></i> Register
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</header>

  <main class="container my-5">
    <!-- Status Messages -->
    <div id="error-message" class="alert alert-danger mb-4" style="display: none;"></div>
    <div id="success-message" class="alert alert-success mb-4" style="display: none;"></div>
    
    <div class="row g-4">
      <!-- Concert Details Column -->
      <div class="col-lg-6">
        <div class="concert-details-card card h-100">
          <!-- Concert Image with Badge -->
          <div class="position-relative">
            <img src="<%= concert.image || '/images/default-concert.jpg' %>" 
                 class="concert-main-image" 
                 alt="<%= concert.name %>">
            <% if (concert.availableTickets < 10) { %>
              <span class="position-absolute top-0 end-0 badge bg-warning text-dark m-3">
                <i class="fas fa-exclamation-circle me-1"></i> 
                <%= concert.availableTickets %> tickets left!
              </span>
            <% } %>
          </div>

          <div class="card-body">
            <h1 class="card-title mb-3" style="font-weight: 700; font-size: 2rem;">
              <%= concert.name %>
              <% if (concert.artist) { %>
                <small class="text-muted d-block mt-2" style="font-size: 1.1rem;">by <%= concert.artist %></small>
              <% } %>
            </h1>

            <div class="concert-meta-grid mb-4">
              <div class="meta-item">
                <i class="fas fa-calendar-day"></i>
                <div>
                  <strong>Date</strong>
                  <p><%= concert.date.toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' }) %></p>
                </div>
              </div>
              
              <div class="meta-item">
                <i class="fas fa-clock"></i>
                <div>
                  <strong>Time</strong>
                  <p><%= concert.time %></p>
                </div>
              </div>
              
              <div class="meta-item">
                <i class="fas fa-map-marker-alt"></i>
                <div>
                  <strong>Venue</strong>
                  <p><%= concert.venue %></p>
                  <% if (concert.city) { %>
                    <small class="text-muted"><%= concert.city %>, <%= concert.country || '' %></small>
                  <% } %>
                </div>
              </div>
              
              <div class="meta-item">
                <i class="fas fa-tag"></i>
                <div>
                  <strong>Price</strong>
                  <p>$<%= concert.ticketPrice.toFixed(2) %> per ticket</p>
                </div>
              </div>
            </div>

            <div class="concert-description mb-4">
              <h5 class="d-flex justify-content-between align-items-center" style="font-weight: 600;">
                About the Event
                <button class="btn btn-sm toggle-description" 
                        type="button" 
                        data-bs-toggle="collapse" 
                        data-bs-target="#descriptionCollapse">
                  Show More <i class="fas fa-chevron-down ms-1"></i>
                </button>
              </h5>
              <div class="collapse" id="descriptionCollapse">
                <p class="mt-3" style="line-height: 1.6;"><%= concert.description || 'No additional details available.' %></p>
              </div>
            </div>

            <div class="additional-details mt-4">
              <% if (concert.genre) { %>
                <span class="badge me-2 mb-2">
                  <i class="fas fa-music me-1"></i> <%= concert.genre %>
                </span>
              <% } %>
              
              <% if (concert.duration) { %>
                <span class="badge me-2 mb-2">
                  <i class="fas fa-hourglass-half me-1"></i> <%= concert.duration %> mins
                </span>
              <% } %>
              
              <% if (concert.ageRestriction) { %>
                <span class="badge me-2 mb-2">
                  <i class="fas fa-user me-1"></i> <%= concert.ageRestriction %>+
                </span>
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <!-- Booking Form Column -->
      <div class="col-md-6">
        <div class="card booking-card">
          <div class="card-header bg-primary text-white">
            <h5 class="mb-0">Book Tickets</h5>
          </div>
          <div class="card-body">
            <% if (user) { %>
              <form action="/bookings/book/<%= concert._id %>" method="POST">
                <input type="hidden" name="concertId" value="<%= concert._id %>">
                <input type="hidden" name="userId" value="<%= user._id %>">

                <div class="mb-3">
                  <label for="name" class="form-label">Full Name</label>
                  <input type="text" class="form-control" id="name" name="name" value="<%= user.name || '' %>" required>
                </div>

                <div class="mb-3">
                  <label for="email" class="form-label">Email</label>
                  <input type="email" class="form-control" id="email" name="email" value="<%= user.email || '' %>"
                    required>
                </div>

                <div class="mb-3">
                  <label for="phone" class="form-label">Phone Number</label>
                  <input type="tel" class="form-control" id="phone" name="phone" required>
                </div>

                <div class="mb-3">
                  <label for="ticketQuantity" class="form-label">Number of Tickets</label>
                  <select class="form-select" id="ticketQuantity" name="ticketQuantity" required
                    onchange="updateTotalPrice()">
                    <% for (let i=1; i <=Math.min(10, concert.availableTickets || 0); i++) { %>
                      <option value="<%= i %>">
                        <%= i %>
                      </option>
                      <% } %>
                  </select>
                </div>

                <!-- price -->
                <div class="mb-3">
                  <label for="totalPrice" class="form-label">Total Price</label>
                  <input type="text" class="form-control" id="totalPrice" name="totalPrice"
                    value="$<%= (concert.ticketPrice * 1)?.toFixed(2) || '0.00' %>" readonly>
                </div>


                <div class="mb-3">
                  <label for="paymentMethod" class="form-label">Payment Method</label>
                  <select class="form-select" id="paymentMethod" name="paymentMethod" required>
                    <option value="credit">Credit Card</option>
                    <option value="debit">Debit Card</option>
                    <option value="paypal">PayPal</option>
                  </select>
                </div>

                <div class="d-grid gap-2">
                  <button type="submit" class="btn btn-success btn-lg">
                    <i class="fas fa-ticket-alt me-2"></i> Confirm Booking
                  </button>
                </div>
              </form>
              <% } else { %>
                <div class="alert alert-warning">
                  <h5 class="alert-heading">Please log in to book tickets</h5>
                  <p>You need to be logged in to make a booking for this concert.</p>
                  <hr>
                  <div class="d-flex justify-content-between">
                    <a href="/login" class="btn btn-outline-primary">Login</a>
                    <a href="/register" class="btn btn-primary">Register</a>
                  </div>
                </div>
                <% } %>
          </div>
        </div>
      </div>
    </div>
  </main>

<footer class="bg-dark text-white py-4 mt-auto footer" style="width: 100vw; margin-left: calc(-50vw + 50%); background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%); border-top: 1px solid rgba(255,255,255,0.1);">
    <div class="container-fluid px-0">
        <div class="text-center px-4">
            <p class="mb-3" style="color: rgba(255,255,255,0.7);">© 2023 ConcertHub. All rights reserved.</p>
            <div class="mt-3 d-flex justify-content-center gap-3">
                <a href="/about" class="text-white-50 text-decoration-none hover-underline" style="transition: all 0.3s ease; color: rgba(255,255,255,0.7) !important;">About Us</a>
                <span style="color: rgba(255,255,255,0.3);">•</span>
                <a href="/contact" class="text-white-50 text-decoration-none hover-underline" style="transition: all 0.3s ease; color: rgba(255,255,255,0.7) !important;">Contact</a>
                <span style="color: rgba(255,255,255,0.3);">•</span>
                <a href="/privacy" class="text-white-50 text-decoration-none hover-underline" style="transition: all 0.3s ease; color: rgba(255,255,255,0.7) !important;">Privacy Policy</a>
                <span style="color: rgba(255,255,255,0.3);">•</span>
                <a href="/terms" class="text-white-50 text-decoration-none hover-underline" style="transition: all 0.3s ease; color: rgba(255,255,255,0.7) !important;">Terms of Service</a>
            </div>
            <div class="mt-3">
                <a href="#" class="text-white-50 mx-2" style="font-size: 1.25rem;"><i class="fab fa-facebook"></i></a>
                <a href="#" class="text-white-50 mx-2" style="font-size: 1.25rem;"><i class="fab fa-twitter"></i></a>
                <a href="#" class="text-white-50 mx-2" style="font-size: 1.25rem;"><i class="fab fa-instagram"></i></a>
                <a href="#" class="text-white-50 mx-2" style="font-size: 1.25rem;"><i class="fab fa-youtube"></i></a>
            </div>
        </div>
    </div>
</footer>

  <!-- JavaScript Libraries -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
  <script>
    // Ticket quantity and price calculation
    document.getElementById('tickets').addEventListener('input', function() {
      const ticketCount = parseInt(this.value);
      const maxTickets = parseInt(this.getAttribute('max'));
      const pricePerTicket = parseFloat(this.dataset.price);
      
      // Validate ticket count
      if (ticketCount > 3) {
        document.getElementById('ticket-error').style.display = 'block';
        this.value = 3;
        return;
      } else {
        document.getElementById('ticket-error').style.display = 'none';
      }
      
      // Calculate total
      const totalPrice = (this.value * pricePerTicket).toFixed(2);
      document.getElementById('totalPrice').textContent = '$' + totalPrice;
    });
    function showMessage(type, text) {
      const elementId = type === 'error' ? 'error-message' : 'success-message';
      const element = document.getElementById(elementId);
      
      element.innerHTML = `<i class="fas fa-${type === 'error' ? 'exclamation-circle' : 'check-circle'} me-2"></i> ${text}`;
      element.style.display = 'block';
      
      setTimeout(() => {
        element.style.display = 'none';
      }, 5000);
    }
  </script>
</body>
</html>